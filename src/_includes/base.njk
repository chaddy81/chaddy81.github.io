<!doctype html>
<html lang="en" class="h-full">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or site.title }} — Software Developer / AI Engineer</title>
  <meta name="description" content="{{ description or site.description }}" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link
    href="https://fonts.googleapis.com/css2?family=Syne:wght@500;700;800&family=Plus+Jakarta+Sans:wght@300;400;500;700&family=IBM+Plex+Mono:wght@400;600&display=swap"
    rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/tokyo-night-dark.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>
  <style>
    :root {
      --bg: #0b0f14;
      --surface: #0f141b;
      --grid: rgba(255, 255, 255, 0.05);
      --fg: #e6e9ef;
      --muted: #8b9bb4;
      --accent: #64ffda;
      --accent-2: #07a9ff;
      --border: #1b2430;
    }

    .font-display {
      font-family: "Syne", ui-sans-serif, system-ui;
    }

    .font-body {
      font-family: "Plus Jakarta Sans", ui-sans-serif, system-ui;
    }

    .font-tech {
      font-family:
        "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Monaco,
        Consolas, "Liberation Mono", "Courier New", monospace;
    }

    @keyframes scanX {
      0% {
        transform: translateX(-40%);
        opacity: 0;
      }

      10% {
        opacity: 1;
      }

      60% {
        opacity: 1;
      }

      100% {
        transform: translateX(130%);
        opacity: 0;
      }
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-6px);
      }
    }

    @keyframes pulse-glow {
      0%, 100% {
        opacity: 0.6;
      }
      50% {
        opacity: 1;
      }
    }

    @keyframes fade-up {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .reveal {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .animate-fade-up {
      animation: fade-up 0.6s ease-out forwards;
    }

    .stagger-1 { animation-delay: 0.1s; }
    .stagger-2 { animation-delay: 0.2s; }
    .stagger-3 { animation-delay: 0.3s; }

    /* Glowing accent line */
    .glow-line {
      position: relative;
    }
    .glow-line::after {
      content: '';
      position: absolute;
      bottom: -4px;
      left: 0;
      width: 100%;
      height: 2px;
      background: linear-gradient(90deg, var(--accent), var(--accent-2));
      border-radius: 2px;
      box-shadow: 0 0 12px var(--accent);
    }

    /* Prose styling for Markdown content */
    .prose {
      color: var(--fg);
    }

    .prose h1,
    .prose h2,
    .prose h3,
    .prose h4 {
      font-family: "Syne", ui-sans-serif, system-ui;
      font-weight: 700;
      color: var(--fg);
      margin-top: 2em;
      margin-bottom: 0.5em;
    }

    .prose h1 {
      font-size: 2.25rem;
      font-weight: 700;
    }

    .prose h2 {
      font-size: 1.5rem;
      font-weight: 600;
    }

    .prose h3 {
      font-size: 1.25rem;
      font-weight: 600;
    }

    .prose p {
      margin-bottom: 1.25em;
      line-height: 1.75;
    }

    .prose a {
      color: var(--accent);
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    .prose a:hover {
      color: var(--accent-2);
    }

    .prose ul,
    .prose ol {
      margin-bottom: 1.25em;
      padding-left: 1.5em;
    }

    .prose li {
      margin-bottom: 0.5em;
    }

    .prose ul li {
      list-style-type: disc;
    }

    .prose ol li {
      list-style-type: decimal;
    }

    .prose blockquote {
      border-left: 3px solid var(--accent);
      padding-left: 1em;
      margin: 1.5em 0;
      color: var(--muted);
      font-style: italic;
    }

    .prose code {
      font-family: "IBM Plex Mono", ui-monospace, monospace;
      background: var(--surface);
      padding: 0.2em 0.4em;
      border-radius: 4px;
      font-size: 0.9em;
    }

    .prose pre {
      background: var(--surface);
      padding: 1em;
      border-radius: 8px;
      overflow-x: auto;
      margin: 1.5em 0;
    }

    .prose pre code {
      background: none;
      padding: 0;
    }

    .prose img {
      border-radius: 8px;
      margin: 1.5em 0;
    }

    .prose hr {
      border: none;
      border-top: 1px solid var(--border);
      margin: 2em 0;
    }

    .prose table {
      width: 100%;
      border-collapse: collapse;
      margin: 1.5em 0;
      font-size: 0.95em;
    }

    .prose th,
    .prose td {
      padding: 0.75em 1em;
      border: 1px solid var(--border);
      text-align: left;
    }

    .prose th {
      background: var(--surface);
      font-family: "Syne", ui-sans-serif, system-ui;
      font-weight: 600;
      font-size: 0.85em;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--accent);
    }

    .prose tr:hover td {
      background: var(--surface)/50;
    }

    .prose strong {
      color: var(--fg);
      font-weight: 600;
    }

    /* Code block enhancements */
    .prose pre {
      position: relative;
      background: var(--surface);
      border: 1px solid var(--border);
      padding: 1.25em;
      border-radius: 12px;
      overflow-x: auto;
      margin: 1.5em 0;
    }

    .prose pre::before {
      content: '';
      position: absolute;
      top: 12px;
      left: 12px;
      display: flex;
      gap: 6px;
    }

    .hljs {
      background: transparent !important;
      padding: 0 !important;
    }
  </style>
</head>

<body class="h-full bg-[var(--bg)] text-[var(--fg)] selection:bg-[var(--accent)]/20 selection:text-[var(--fg)]">
  <!-- Background layers -->
  <div class="fixed inset-0 -z-10">
    <!-- Soft radial ambience -->
    <div class="absolute inset-0 bg-[radial-gradient(1200px_700px_at_120%_-20%,rgba(100,255,218,0.12),transparent)]">
    </div>
    <div class="absolute inset-0 bg-[radial-gradient(800px_500px_at_-20%_20%,rgba(7,169,255,0.12),transparent)]"></div>
    <!-- Subtle grid -->
    <div
      class="absolute inset-0 opacity-[0.18] mix-blend-screen bg-[length:36px_36px] bg-[linear-gradient(var(--grid)_1px,transparent_1px),linear-gradient(90deg,var(--grid)_1px,transparent_1px)]">
    </div>
    <!-- Tiny noise texture -->
    <div
      class="absolute inset-0 opacity-[0.08] pointer-events-none bg-[url('data:image/svg+xml;utf8,<svg xmlns=%22http://www.w3.org/2000/svg%22 width=%2240%22 height=%2240%22 viewBox=%220 0 40 40%22><filter id=%22n%22><feTurbulence type=%22fractalNoise%22 baseFrequency=%220.8%22 numOctaves=%222%22 stitchTiles=%22stitch%22/></filter><rect width=%2240%22 height=%2240%22 filter=%22url(%23n)%22 opacity=%220.25%22/></svg>')]">
    </div>
  </div>

  <!-- Top nav -->
  <header class="relative">
    <div
      class="mx-auto max-w-[1200px] py-5 md:py-6 px-6 flex items-center justify-between border-b border-[var(--border)]/70 backdrop-blur-sm bg-[var(--bg)]/30">
      <a href="/" class="group flex items-center gap-3">
        <!-- Monogram -->
        <div class="relative w-10 h-10 flex items-center justify-center">
          <div class="absolute inset-0 bg-gradient-to-br from-[var(--accent)] to-[var(--accent-2)] rounded-lg opacity-20 group-hover:opacity-30 transition-opacity"></div>
          <span class="font-display font-bold text-lg text-[var(--accent)] group-hover:text-[var(--fg)] transition-colors">CB</span>
        </div>
        <div class="leading-tight">
          <div class="font-display font-bold tracking-tight text-lg group-hover:text-[var(--accent)] transition-colors">
            CHAD BARTELS
          </div>
          <div class="font-tech text-[10px] uppercase tracking-[0.15em] text-[var(--muted)]">
            Engineering & AI
          </div>
        </div>
      </a>
      <nav class="hidden md:flex items-center gap-6">
        <a href="/#posts" class="font-tech text-[12px] uppercase tracking-[0.1em] text-[var(--muted)] hover:text-[var(--accent)] transition-colors">Posts</a>
        <a href="https://github.com/chaddy81" target="_blank" rel="noopener" class="font-tech text-[12px] uppercase tracking-[0.1em] text-[var(--muted)] hover:text-[var(--accent)] transition-colors">GitHub</a>
        <a href="https://linkedin.com/in/chadbartels" target="_blank" rel="noopener" class="font-tech text-[12px] uppercase tracking-[0.1em] text-[var(--muted)] hover:text-[var(--accent)] transition-colors">LinkedIn</a>
      </nav>
    </div>
  </header>

  {{ content | safe }}

  <!-- Footer -->
  <footer class="relative border-t border-[var(--border)]/50">
    <div class="mx-auto max-w-[1200px] px-6 py-12 md:py-16">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-8">
        <div>
          <div class="font-display font-bold text-lg mb-2">Chad Bartels</div>
          <p class="font-body text-sm text-[var(--muted)] max-w-md">
            VP of Engineering, AI tinkerer, and chronic automator. Writing about the things I'm building and learning.
          </p>
        </div>
        <div class="flex items-center gap-6">
          <a href="https://github.com/chaddy81" target="_blank" rel="noopener" class="group flex items-center gap-2 text-[var(--muted)] hover:text-[var(--accent)] transition-colors">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            <span class="font-tech text-[11px] uppercase tracking-wider">GitHub</span>
          </a>
          <a href="https://linkedin.com/in/chadbartels" target="_blank" rel="noopener" class="group flex items-center gap-2 text-[var(--muted)] hover:text-[var(--accent)] transition-colors">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
            <span class="font-tech text-[11px] uppercase tracking-wider">LinkedIn</span>
          </a>
        </div>
      </div>
      <div class="mt-10 pt-6 border-t border-[var(--border)]/30 flex flex-col md:flex-row md:items-center md:justify-between gap-4">
        <p class="font-tech text-[11px] text-[var(--muted)]/60 uppercase tracking-wider">
          &copy; {{ '' | year }} Chad Bartels. Built with care.
        </p>
        <div class="flex items-center gap-2">
          <span class="inline-block w-2 h-2 rounded-full bg-[var(--accent)] animate-[pulse-glow_2s_ease-in-out_infinite]"></span>
          <span class="font-tech text-[11px] text-[var(--muted)]/60 uppercase tracking-wider">Always building</span>
        </div>
      </div>
    </div>
  </footer>

  <!-- Scroll reveal script -->
  <script>
    // Initialize syntax highlighting
    hljs.highlightAll();

    // Scroll reveal
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

    reveals.forEach(el => observer.observe(el));
  </script>
</body>

</html>
